<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width,initial-scale=1" />
        <title>GREENSCAN</title>
        <link rel="stylesheet" type="text/css" href="main.css" />
        <script src="http://code.jquery.com/jquery-latest.js"></script>
    </head>
    <body>
        <header>
            <div>
                <a href="/">GREENSCAN</a>
            </div>
        </header>
        <nav>
            <a class="nav_bar" href="/barcode">BARCODE SCAN</a>
            <a class="nav_search" href="/search">SEARCH</a>
            <a class="nav_map" href="/map">MAP</a>
        </nav>
        <div class="main_content">
            <h3 class="current_page">BARCODE SCAN RESULT</h3>
            <div class="item_box current_item">
                <p class="line"></p>
                <p class="sub_title"><%= name %></p>
                <% if (imgSrc == null || imgSrc == "/images/common/no_img.gif") { %>
                <% if (isGreen) { %>
                <img class="item_img" onclick="location.href='https://www.coupang.com/np/search?component=&q=<%= name %>&channel=user'" src=isGreen.jpg />
                <% } else { %>
                <img class="item_img" onclick="location.href='https://www.coupang.com/np/search?component=&q=<%= name %>&channel=user'" src=isNotGreen.jpg />
                <% }} else { %>
                <img class="item_img" onclick="location.href='https://www.coupang.com/np/search?component=&q=<%= name %>&channel=user'" src=<%= imgSrc %> />
                <% } %>
                <% if (isGreen) { %>
                <p class="result green">녹색제품이 맞습니다.</p>
                <% } else { %>
                <p class="result non_green">녹색제품이 아닙니다.</p>
                <% } %>
            </div>
            <div class="recommend">
                <p class="line"></p>
                <p class="sub_title">[Recommend]</p>
                <div class="recommend_list">
                <% if (greenItems.length > 0) { %>
                    <% let count = 0; %>
                    <%  for (greenItem of greenItems) { %>
                        <% count++; %>
                        <div class="item_box">
                            <img src="leaf<%= count % 5 %>.png" alt="leaf" class="leaf_img">
                            <p class="item_title">
                                <a href="https://www.coupang.com/np/search?component=&q=<%= greenItem.모델명 %>&channel=user"><%= greenItem.모델명 %></a>
                            </p>
                        </div>
                    <% } %>
                <% } else { %>
                    <p class="item_title">
                        추천 녹색제품이 없습니다.
                    </p>
                <% } %>
                </div>
            </div>
        </div>
    </body>
</html>
